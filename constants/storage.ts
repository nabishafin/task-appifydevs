/** localStorage keys. Kept in one place so they never drift between modules. */
export const STORAGE_KEYS = {
  theme: "echogpt-theme",
  preferences: "echogpt-preferences",
  chat: "echogpt-chat",
  prompts: "echogpt-prompts",
  ui: "echogpt-ui",
  extension: "echogpt-extension",
} as const;
